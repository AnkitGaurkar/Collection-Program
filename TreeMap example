/*TreeMap:-
 * TreeMap class is Red-Black tree based implementation..
 * It is an provides an efficient means of storing key value pair in Sorted order..
 * 
 * TreeMap contain values on the basis keys.
 * TreeMap contain only unique elements (key and values).
 * TreeMap cannot have null key but can have multiple null values..
 * It is non Synchronized.
 * Its is maintain Ascending Order....
 */
package com;

import java.util.Map;
import java.util.TreeMap;

public class TreeMapEx {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		TreeMap<Integer,String> t1=new TreeMap<Integer,String> ();
		t1.put(3, "SUnil");
		t1.put(2, "Rohan");
		t1.put(4, "Ankit");
		t1.put(3, "Sunil");
		
		for(Map.Entry<Integer, String>entry:t1.entrySet())
		{
			System.out.println(entry.getKey()+" "+entry.getValue());
		}
	}

}
